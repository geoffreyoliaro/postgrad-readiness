# Local development (copy to .env if your tooling loads it)
PORT=3001
HOST=0.0.0.0
DB_FILE=postgrad-eval.db

# Railway production (set in the API service Variables tab)
# PORT=                    # provided automatically by Railway
# HOST=0.0.0.0
# DB_FILE=/data/postgrad-eval.db   # mount a Volume at /data (see docs/RAILWAY.md)
